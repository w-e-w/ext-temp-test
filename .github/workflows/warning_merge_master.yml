name: Pull requests can't be merge into master 

"on":
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
    branches:
      - test-protection-branch-2
jobs:
  redo:
    runs-on: ubuntu-latest
    steps:
      - name: Warning marge into master
        run: |
          echo -e "::warning::This pull request directly merge into \"master\" branch, normally development happens on \"dev\" branch."
          exit 1
